// Populated once by the manual EAS bootstrap workflow.
// Keeping the project ID in a dedicated file lets Expo's dynamic app config
// remain deterministic and makes the one-time linkage easy to review.
export const EAS_PROJECT_ID: string | null = null;
